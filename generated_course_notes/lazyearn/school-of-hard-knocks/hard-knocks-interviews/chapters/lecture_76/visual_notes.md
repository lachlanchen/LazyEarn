# Visual Evidence
## Frame Inventory
- `lecture_76_figure_02.png`: A sponsor-segment frame showing the host beside an overlaid `fabric by Gerber Life` app mockup with a visible child gift card example, and it should remain in the final notes as a screenshot because the concrete UI is the evidence.

## Equation Extraction
- No blackboard equations or formal mathematical notation are visible in this frame.
- `\$25` [visible]
- No other monetary expression should be extracted from the image itself; the nearby transcript mention of `\$1` per day is not visible here and should not be promoted to frame-derived notation.

## Diagram Extraction
- The frame does not contain a blackboard sketch, state diagram, axes, or technical board layout.
- The visible structure is a two-part presentation layout:
  - left side: a phone-style app mockup with brand header, recipient fields, amount, message area, and `SEND GIFT` button;
  - right side: the host speaking to camera while holding a phone.
- This should be preserved primarily as a screenshot, not redrawn as a formal technical diagram.
- If the notes need a simplified explanatory graphic, a very small secondary flow diagram could be redrawn in TikZ as:
  family/friends → gifting link → child investment account → later uses,
  but that redraw would be a cautious conceptual aid, not a replacement for the original screenshot.

## Reconstruction Guidance
- Keep `lecture_76_figure_02.png` visible in the final notes because the evidentiary value lies in the actual interface, branding, recipient labeling, and visible `\$25` example.
- Do not convert this frame into a displayed equation; there is no genuine equation content to reconstruct.
- Do not redraw the full UI in TikZ unless there is a compelling layout need in a later compiled volume; even then, the screenshot should remain nearby as the source-facing artifact.
- In prose, use the screenshot to support a concrete mechanism: birthday gifts are being reframed as contributions into a child-focused investment pathway rather than as disposable toys.
- Restrict any note-quality extraction to clearly legible elements such as `fabric by Gerber Life`, `To: Charlie`, `From: Grandma & grandpa`, `\$25`, and `SEND GIFT`.

## Uncertainties
- The body text inside the gift message is only partly legible and should not be quoted in full without checking a higher-resolution source.
- The gray top placeholders and the purple bar near the bottom look like UI elements, but their exact function is not reliably inferable from this frame alone.
- The subtitle is truncated mid-sentence, so the full claim should come from the transcript rather than the screenshot.
- There is no visible board structure, no axes, and no derivation to reconstruct from the image.