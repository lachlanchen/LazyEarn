# Visual Evidence
## Frame Inventory
- `lecture_03_figure_02.png`: Close blackboard shot from the probability-review segment, showing a large central-limit-theorem heading and abbreviated assumptions about identically distributed random variables and finite variance; this screenshot should remain in the final notes as visual evidence.

## Equation Extraction
- `lecture_03_figure_02.png`
  - [partially visible] `\text{C. lim.\ theorem}`
  - [partially visible] `\text{ident.\ d.d.\ random variable}`
  - [partially visible] `\text{finite Var}`
  - [standard completion] `X_1,\dots,X_n \text{ are independent and identically distributed random variables}`
  - [standard completion] `\operatorname{Var}(X_i) < \infty`
  - [standard completion] `\bar X_n \text{ is approximately normally distributed for large } n`
  - [standard completion] `\frac{\bar X_n-\mu}{\sigma/\sqrt{n}} \Rightarrow N(0,1)`

## Diagram Extraction
- No plotted diagram, axis system, bell curve, or flow sketch is directly visible in this frame.
- The useful visual structure is the blackboard layout itself: a theorem heading across the upper line, followed by stacked abbreviated assumption lines below.
- This frame should be preserved as a screenshot rather than redrawn in TikZ.
- No TikZ figure is required from this asset alone.

## Reconstruction Guidance
- Keep `lecture_03_figure_02.png` in the chapter beside the probability-review discussion, because it gives direct evidence that Shiller paused to restate the central limit theorem and its assumptions on the board.
- Do not treat the chalk writing as a verbatim source for full notation. The board supports the theorem title and the presence of assumptions, but not a fully explicit formal statement.
- In the clean notes, reconstruct the theorem cautiously in standard mathematical form: i.i.d. random variables, finite variance, and convergence of normalized averages to a normal law.
- Present the polished theorem as a standard completion grounded by both frame and transcript, and keep the screenshot nearby so the reader can see the original blackboard emphasis and ordering.
- Since no bell curve is visible, any normal-distribution sketch should come only from transcript-driven exposition, not as a claimed redraw of this frame.

## Uncertainties
- The heading is abbreviated and may read `C. lim. theorem`, but the exact chalk shorthand is not perfectly secure.
- The middle line is cropped on the left, and the abbreviation after `ident.` is unclear; it strongly suggests shorthand for “identically distributed.”
- The chalk appears to read `random variable` in the singular, though this may just be truncation or incomplete writing.
- The lower line preserves only part of `finite Var`, so `finite variance` is a cautious completion rather than a fully direct transcription.
- No indices, means, variances, or asymptotic arrow are actually visible on the board; those belong to careful standard reconstruction from the lecture context, not direct extraction from the image.
- The prompt refers to Leonard Susskind, but the actual lecture source here is Robert J. Shiller’s `Financial Markets`; the note brief should follow that source.