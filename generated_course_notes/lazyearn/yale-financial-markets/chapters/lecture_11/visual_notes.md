# Visual Evidence
## Frame Inventory
- `lecture_11_figure_02.png`: Blackboard setup with `Value Function`, `Weighting Function?`, and partial `prospect theory / Kahneman & Tversky`; keep this screenshot in the final notes because it documents the lecture's two-part board structure before the graphs are drawn.
- `lecture_11_figure_03.png`: Central prospect-theory value-function sketch with axes, `Value`, `losses`, `gains`, and the asymmetric curve; keep this screenshot and also pair it with a clean TikZ redraw.
- `lecture_11_figure_04.png`: Transition board for the weighting-function discussion, with `Weightin` in the center and contextual notes on `funeral insurance`, `framing`, `reference pt`, and `Today's Wealth`; keep this screenshot as documentary context, not as a direct graph source.

## Equation Extraction
- [visible] `\text{Value Function}`
- [visible] `\text{Weighting Function?}`
- [partially visible] `\text{prospect theory}`
- [partially visible] `\text{Kahneman \& Tversky}`
- [visible] `\text{Value}`
- [visible] `\text{losses}`
- [visible] `\text{gains}`
- [partially visible] `\text{Weightin}`
- [partially visible] `\text{funeral insurance}`
- [visible] `\text{framing}`
- [visible] `\text{reference pt}`
- [visible] `\text{Today's Wealth}`
- [standard completion] `v(x)` as cautious note-quality notation for the value function drawn in `lecture_11_figure_03.png`
- [standard completion] `x=0` for the origin / reference point at the center of the value-function axes
- [standard completion] `w(p)` as cautious note-quality notation for the weighting function introduced after `lecture_11_figure_04.png`
- [standard completion] `p \in [0,1]` for probability, since the transcript explicitly frames the weighting function on the interval from `0` to `1`

## Diagram Extraction
- `lecture_11_figure_02.png` is not a finished mathematical diagram; it should remain as a screenshot only, because its main value is the board layout and the explicit pairing of `Value Function` with `Weighting Function?`.
- `lecture_11_figure_03.png` contains the one figure that should definitely be redrawn in TikZ: centered axes, `Value` on the vertical axis, `losses` to the left, `gains` to the right, and an asymmetric prospect-theory value curve with a steeper loss side and a flatter gain side.
- The value-function figure should be shown both ways: the screenshot as historical board evidence, and the TikZ redraw as the clean analytical version for the notes.
- `lecture_11_figure_04.png` does not contain the weighting-function graph itself; it should remain as a screenshot because it captures the transition into the weighting-function discussion and the surrounding interpretive notes about framing and the reference point.
- If the final chapter includes a weighting-function TikZ figure, that redraw should be treated as transcript-backed reconstruction rather than direct frame transcription, and it should be placed near `lecture_11_figure_04.png`.
- Across the three frames, the board has a meaningful left-center-right structure: left panel for retained headings, center for the active diagram or new heading, and right panel for contextual application notes.

## Reconstruction Guidance
- Use the screenshots to preserve board layout, labels, and sequencing, and use the transcript to clarify what the axes and sketches mean.
- For the value function, redraw only the qualitative structure that is actually supported: a vertical `Value` axis, a horizontal gains-losses axis, a central origin, concavity on the gains side, convexity on the losses side, and a sharp change in slope at the origin.
- Do not assign numerical scales, fitted parameters, or textbook functional forms to the value-function redraw, because none of that is visible on the board.
- Keep `lecture_11_figure_03.png` adjacent to the TikZ redraw so the notes show both the lecture artifact and the cleaned mathematical version.
- Treat `v(x)`, `w(p)`, `x=0`, and `p \in [0,1]` as cautious standard completions for note-writing convenience, not as chalkboard transcriptions.
- There is no physics derivation in these frames, so “standard completion” here should mean only minimal prospect-theory notation and diagram cleanup, not imported formulas from outside the lecture.
- For the weighting function, do not pretend the graph comes from `lecture_11_figure_04.png`; if drawn, present it as a transcript-guided interpretive sketch and keep the screenshot nearby as evidence of the board context.
- The right-side notes in `lecture_11_figure_04.png` are better converted into explanatory prose or a short note block on framing, reference points, and why small salient risks get overinsured.

## Uncertainties
- `\text{Kahneman \& Tversky}` is cropped and softened at the right edge in `lecture_11_figure_02.png`, so the full surname lettering is not visually secure from the frame alone.
- `\text{prospect theory}` is legible in substance, but the watermark and glare interfere with exact letter shapes.
- The exact geometry of the kink in `lecture_11_figure_03.png` is not precise enough to justify a literal point-for-point redraw.
- The origin is not visibly labeled `0` in `lecture_11_figure_03.png`; the transcript says the zero was erased, so `x=0` is a standard completion rather than a visible inscription.
- `\text{Weightin}` in `lecture_11_figure_04.png` is incomplete, so the finished heading must be inferred from the lecture flow.
- `\text{funeral insurance}`, `\text{reference pt}`, and `\text{Today's Wealth}` are readable in substance but faint and partly softened by erasure marks.
- No completed weighting-function graph appears in the provided frames, so any later `w(p)` figure must be labeled as a cautious reconstruction rather than a direct extraction.
- No tick marks, parameter values, or explicit functional equations are visible anywhere on the board, so the notes should avoid overclaiming mathematical precision.