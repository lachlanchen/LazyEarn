# Visual Evidence
## Frame Inventory
- `lecture_23_figure_02.png`: a chalkboard frame showing an unlabeled steep upward curve on axes at the left and residual text `Unger 1996` and `www.unicef.org` on the right; this screenshot should remain in the final notes because it is the only frame-backed mathematical sketch and it also records the mixed board state at the Malthus pivot.

## Equation Extraction
- `lecture_23_figure_02.png`: [visible] `\text{Unger 1996}`
- `lecture_23_figure_02.png`: [visible] `\texttt{www.unicef.org}`
- `lecture_23_figure_02.png`: [standard completion] `y \propto e^{kt}` as a cautious mathematical rendering of the visibly convex growth curve
- `lecture_23_figure_02.png`: no explicit symbolic equation is written on the board; the mathematical content is primarily graphical

## Diagram Extraction
- The left side of `lecture_23_figure_02.png` contains a simple chalk graph with one vertical axis, one horizontal axis, and one sharply rising convex-upward curve.
- This graph should be shown both ways: preserved as the original screenshot and redrawn in TikZ nearby.
- The TikZ redraw should reproduce only what is actually visible: unlabeled axes and a single steep curve.
- The right side of the screenshot should not be redrawn as part of the Malthus graph, but it should remain visible in the screenshot because it documents residual board text from the earlier Unger–UNICEF discussion.

## Reconstruction Guidance
- Keep `lecture_23_figure_02.png` visible in the hopelessness or Malthus section of the final chapter as the evidentiary anchor for the live chalkboard sketch.
- Add a clean TikZ redraw of the left-hand graph immediately nearby so the notes have a readable diagram independent of the photograph.
- Do not invent axis labels, scales, or numerical annotations, because none are visible in the frame.
- Do not add the linear “arithmetical” comparison line into the TikZ redraw of this screenshot itself, since that second curve is not actually visible here.
- If the chapter later formalizes Malthus’s argument more fully, it is reasonable to add a separate transcript-backed reconstruction such as exponential population growth against a linear subsistence path, but that should be clearly presented as interpretation rather than direct board transcription.
- Treat `Unger 1996` and `www.unicef.org` as board text worth preserving in the screenshot, not as mathematical content to weave into the Malthus derivation.

## Uncertainties
- The right-side text appears to be leftover from the earlier morality discussion rather than part of the new growth sketch.
- `Unger 1996` is legible, but its exact bibliographic significance is uncertain because the transcript is noisy around Unger’s dates.
- There are no visible axis labels, so any identification of variables such as population, time, or output would have to come from the transcript rather than the image.
- The frame shows only one steep convex curve; the linear “arithmetical” comparison mentioned in speech is not visible.
- A faint chalk mark appears after `www.unicef.org`, but it is too unclear to treat as meaningful notation.