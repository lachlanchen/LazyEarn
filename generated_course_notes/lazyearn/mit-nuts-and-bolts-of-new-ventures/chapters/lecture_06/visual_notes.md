# Visual Evidence
## Frame Inventory
- `lecture_06_figure_02.png`: a yellow slide titled `Potential audiences for your pitch` with a horizontal divider and two visible bullets, `Customers` and `Potential employees`; this screenshot should remain in the final notes as visual evidence for the lecture’s audience-segmentation step and its incremental reveal state.

## Equation Extraction
- [visible] None. This frame contains no equations, symbolic expressions, axes, or algebraic notation.
- [standard completion] None needed. The image is a text slide rather than a board derivation or mathematical sketch.

## Diagram Extraction
- The visible structure is a simple list layout rather than a true diagram: title at top, horizontal rule beneath it, then a vertically stacked bullet list.
- The important visual fact is that this is a partial slide state, not the fully populated audience list. Only `Customers` and `Potential employees` are visible in this frame.
- This frame should be preserved primarily as a screenshot, not redrawn as a standalone TikZ figure.
- If the final chapter later wants a compact audience tree or flowchart, that redraw should be treated as transcript-derived rather than frame-derived, and the screenshot should stay nearby as the visual anchor.

## Reconstruction Guidance
- Do not invent any mathematics from this image; there is none to reconstruct.
- If the notes need a clean textual reconstruction alongside the figure, reproduce only the content that is directly visible in the frame:
  - `Customers`
  - `Potential employees`
- Do not expand this frame into the full later audience list unless that expansion is explicitly sourced from the transcript and clearly presented as transcript-backed rather than image-backed.
- Keep the screenshot when discussing the turn from generic “presenting your venture” to audience-specific pitch strategy, because the staged slide reveal is itself useful evidence of lecture pacing.
- No TikZ redraw is necessary for this asset alone. If a later narrow flowchart is added for readability, it should summarize the transcript’s logic, not pretend to reproduce a diagram from the slide.

## Uncertainties
- The footer text is faint and appears to read `Copyright © 2025 Bob Jones, All rights reserved`, but it is not fully crisp enough to treat as central content.
- The upper-right logo mark is visible but functions as branding, not conceptual evidence.
- This frame does not show the later bullets discussed in the transcript, so it must be treated as a partial reveal state.
- There are no visible symbols, indices, formulas, or board marks to disambiguate.