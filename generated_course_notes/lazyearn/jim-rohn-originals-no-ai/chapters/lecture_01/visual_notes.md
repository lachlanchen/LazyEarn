# Visual Evidence
## Frame Inventory
- `lecture_01_figure_03.png`: Jim Rohn is writing on the board; the underlined heading clearly ends with `Diseases`, and the next line begins with `Ind`, so this screenshot should remain in the final notes as evidence for the start of the chalkboard list.
- `lecture_01_figure_04.png`: Close-up of the board with `pessimism` clearly visible beneath older, partially legible chalk writing, and this screenshot should remain in the final notes as evidence for one later item in the same list.

## Equation Extraction
- No mathematical equations are visible in these frames; the board contains labels, list entries, and emphasis marks rather than derivations.
- `\underline{\text{Diseases}}` `[visible]`
- `\text{Ind}` `[visible]`
- `\text{indifference}` `[standard completion]`
- `\text{pessimism}` `[visible]`
- `(\text{timid})` `[partially visible]`

## Diagram Extraction
- `lecture_01_figure_03.png`: This is not a formal diagram; it is a live-built board list with a top heading and the first item beginning underneath. Preserve the screenshot, and if needed accompany it with a clean typeset list fragment rather than a TikZ redraw.
- `lecture_01_figure_04.png`: This is a stacked board layout, not a geometric sketch or state diagram. Preserve the screenshot, and if needed show a clean typeset line for `\text{pessimism}` nearby; no TikZ is warranted.
- Taken together, the two frames show board sequencing: first the list is opened, then a later disease entry is added. That sequencing is worth preserving visually, but it should not be turned into an invented flowchart.

## Reconstruction Guidance
- Treat these frames as textual board evidence, not as mathematical figures. The clean note version should use plain LaTeX text displays, short aligned list items, or subsection headings rather than equations or TikZ.
- For `lecture_01_figure_03.png`, keep the screenshot visible and, if the chapter needs a legible companion rendering, typeset a minimal reconstruction such as `\text{Diseases}` followed by `\text{indifference}`. The completion from `Ind` to `indifference` should be understood as transcript-assisted, not fully board-visible.
- For `lecture_01_figure_04.png`, keep the screenshot visible and typeset only the securely readable `\text{pessimism}` as the clean companion line. Do not promote the surrounding cropped chalk into a more definite structure than the image supports.
- Do not use “standard physics knowledge” style completion here; there is no mathematical or physical derivation to recover. The only acceptable completions are cautious textual ones supported by the transcript and the visible letterforms.

## Uncertainties
- In `lecture_01_figure_03.png`, the word before `Diseases` is not reliably readable because the speaker blocks part of the board.
- In `lecture_01_figure_03.png`, only `Ind` is directly visible; completing it to `indifference` is a careful transcript-based completion, not a full transcription of the frame.
- In `lecture_01_figure_04.png`, the parenthetical middle word appears to be `timid`, but the chalk is soft and the reading is not certain enough to use without qualification.
- In `lecture_01_figure_04.png`, the top cropped line and the looping word on the right are too incomplete to transcribe confidently.
- The short horizontal chalk strokes in both frames look like emphasis marks or separators, not equals signs or other formal operators.