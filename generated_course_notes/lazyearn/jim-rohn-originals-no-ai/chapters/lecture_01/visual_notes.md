# Visual Evidence
## Frame Inventory
- `lecture_01_figure_02.png` shows Jim Rohn writing on the board, with a broad cursive heading across the top and the start of the first list entry beneath it; this screenshot should remain in the final notes because it is the clearest visual evidence for the lecture’s transition into the disease list.
- `lecture_01_figure_03.png` shows a close-up chalkboard stack with `pessimism` clearly legible near the bottom and cropped labels above it; this screenshot should also remain in the final notes and may be paired with a cautious TikZ redraw for legibility.

## Equation Extraction
- [partially visible] `\text{... Diseases}`
- [standard completion] `\text{Attitude Diseases}`
- [standard completion] `\text{Indifference}`
- [visible] `\text{pessimism}`
- [partially visible] `(\text{...})`
- [partially visible] `\text{... approach}`
- [standard completion] `\text{timid approach}`
- [standard completion] `\text{over caution}`

There is no reliably visible mathematical equation in these frames. The chalk marks in `lecture_01_figure_03.png` are better treated as stacked labels plus separator strokes than as confirmed algebra.

## Diagram Extraction
- `lecture_01_figure_02.png` should be preserved as a screenshot only. Its value is board organization: a title line across the top and the beginning of a list underneath, not a redraw-worthy formula.
- `lecture_01_figure_03.png` can be shown both ways: keep the original screenshot visible and, if helpful, redraw a narrow vertical text stack in TikZ. The redraw should use text nodes and short separator strokes only, with no arrows or formal equalities unless another frame confirms them.
- The most important layout fact to preserve is sequence. `lecture_01_figure_03.png` appears to retain earlier disease labels above the newly visible `pessimism`, so the final notes should reflect a board accumulating items over time rather than a single isolated word.

## Reconstruction Guidance
- Treat `lecture_01_figure_02.png` as documentary evidence for the list heading. In the chapter text, reconstruct the heading and the first item as ordinary prose or a numbered list, but do not pretend the full chalk text is directly legible.
- Treat `lecture_01_figure_03.png` as a partially legible board stack. If redrawn, keep `pessimism` explicit and render the upper items only as cautious transcript-assisted completions or as partial labels.
- Do not import extra formalism or “standard physics knowledge” here. This lecture is not doing physics or mathematics on the board; the usable reconstruction is conceptual and typographic, not derivational.
- Do not silently convert the short double horizontal strokes into `=`. If a clean note version needs separators, use small horizontal rules or visual spacing rather than algebraic notation.
- Keep the screenshot close to any redraw whenever the redraw includes completed labels like `Attitude Diseases`, `Indifference`, `over caution`, or `timid approach`, since those completions rely partly on transcript context rather than direct legibility.

## Uncertainties
- In `lecture_01_figure_02.png`, the first word of the heading is not fully readable from the image alone, and the first list item is mostly hidden by the speaker.
- In `lecture_01_figure_03.png`, the upper labels are cropped and blurred, so `over caution` and `timid approach` should be understood as cautious transcript-backed completions rather than secure visual transcriptions.
- The short double strokes between labels in `lecture_01_figure_03.png` may be underlines, separators, or equality-like marks; their exact semantic role is not reliable from the frame alone.
- The right-side chalk fragments in `lecture_01_figure_03.png` are too incomplete to expand confidently into full words without overreach.