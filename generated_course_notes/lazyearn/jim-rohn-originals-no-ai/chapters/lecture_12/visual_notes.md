# Visual Evidence
## Frame Inventory
- `lecture_12_figure_01.png`: Wide opening seminar shot with Jim Rohn beside a large green chalkboard whose top heading reads as `Personal Dev.`; this screenshot should remain in the final notes as contextual board evidence.
- `lecture_12_figure_03.png`: Later chalkboard comparison shot with a visible minus sign on the left, a plus sign on the right, and a partially legible underlined word ending in `use`; this screenshot should remain in the final notes and should also support a cautious redraw.

## Equation Extraction
- [visible] \(\text{Personal Dev.}\)
- [standard completion] \(\text{Personal Development}\)
- [visible] \(-\)
- [visible] \(+\)
- [partially visible] \(\underline{\text{\dots use}}\)
- [standard completion] \(\text{Whatever you don't use, you lose.}\)
- [standard completion] \(\text{lack of use} \Rightarrow \text{loss}\)

## Diagram Extraction
- `lecture_12_figure_01.png` is not a mathematical diagram; it preserves the opening board structure as a titled but mostly blank seminar board, so it should be kept as a screenshot only.
- `lecture_12_figure_03.png` shows a two-sided board comparison rather than a full derivation. The visible `-` and `+` markers suggest a negative/positive contrast, so this should be shown both ways: keep the screenshot and add a narrow TikZ or tabular redraw nearby.
- No axes, arrows, graphs, or multi-step board derivations are visible in either frame.
- The redraw for `lecture_12_figure_03.png` should be a compact vertical comparison, not a wide flowchart.

## Reconstruction Guidance
- Use `lecture_12_figure_01.png` near the opening of the chapter to establish the seminar setting and the governing topic of personal development. Do not fabricate equations around it; the useful evidence is the heading and board presence.
- For `lecture_12_figure_03.png`, keep the original screenshot as the visual source and place a clean companion comparison next to it. The redraw should use the transcript-backed law of use rather than pretend to transcribe the obscured chalk text exactly.
- A safe redraw is a two-column or stacked comparison with `-` and `+` headings and short labels such as `unused -> loss` and `use / employ -> keep / enlarge`. These labels are note-quality completions, not literal board transcriptions.
- If a displayed equation is added beside `lecture_12_figure_03.png`, it should be explicitly transcript-led, for example \(\text{Whatever you don't use, you lose.}\), rather than presented as fully legible chalkboard text.
- Keep all reconstructed visuals pocket-safe: narrow layout, wrapped labels, no dense horizontal spread, and no attempt to reconstruct hidden intermediate words.

## Uncertainties
- In `lecture_12_figure_01.png`, the heading is soft enough that `Personal Dev.` is safer than claiming a fully legible `Personal Development` directly from the frame.
- In `lecture_12_figure_03.png`, the underlined word is only partially readable; the ending strongly suggests `use`, but the full word or phrase is obscured.
- The exact semantic assignment of the visible `-` and `+` to specific chalk phrases is not fully recoverable from the image alone.
- No complete equation is directly legible in either frame, so all law-of-use statements should be treated as standard completion from the transcript rather than literal chalkboard transcription.
- There are no visible indices, arrows, or multi-line symbolic steps to preserve; the evidence is structural and rhetorical rather than formally mathematical.