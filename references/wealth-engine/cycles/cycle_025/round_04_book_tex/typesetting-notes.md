# TeX typesetting notes — Cycle 25 round 4

Date: 2026-04-06

- Kept the chapter flow in place and added a new section-level insertion between existing sections in Chapter 3 and `\chapter{What wealth is}` to avoid numbering disruption.
- Chose `\section` rather than `\subsection` for direct parity with surrounding top-level chapter sections in this file.
- Reused existing `notebox` and `signal` color styles to keep styling consistent with current book voice.
- Table uses `tabularx` with compact column width balancing and `\small` display for dense comparison rows.
- Kept content ASCII-only and avoided adding new custom macros.
- Added a practical interpretation callout as a visible reader-facing enhancement, not just structural parity.
