# Cycle 020, round 06: readme sync notes

- Scope: keep README and repo-facing state aligned to mission cycle and visible book work.
- Changes made:
  - Updated `README.md` top-level sync state from `cycle_019` to `cycle_020`.
  - Added `Cycle_020 operating focus` with explicit callouts to `9.11.5` work and no-content-edit guard.
  - Added `Book sync highlights (cycle_020)` describing the 9.11.5 markdown/TeX alignment and source-note updates.
  - Added a `Cycle 020 readme_sync follow-up` section in `next-tasks.md` to carry the cycle-label drift and sync scope forward.
- Verification basis:
  - README line-level checks against existing `round_03_book_markdown` and `round_04_book_tex` summary content.
  - `docs/index.html`, `docs/script.js`, and `docs/translations.json` still show `Cycle_019` research copy, so this round remains README-only by design.
- Suggested next action:
  - In the next website-facing pass, update runtime research-cycle language to `Cycle_020` only after validating that `U86` evidence linkage remains readable in the public route copy.
