# Cycle 008 / Round 03 - book notes

Date: 2026-04-05

## Reader-facing markdown improvements

1. Added new section `9.7 Entry-and-property-price pulse (monthly and quarterly)` in:
   - `investment/wealth-from-first-principles.md`

2. New content added in `9.7`:
   - one operational explanation focused on avoiding false inference from entry or valuation signals,
   - one six-row method table,
   - one four-step sequence rule,
   - one explicit defensive action rule when entry is high but durability/liquidity conditions are weak.

3. Expanded curated references in `11.5 Official data and references`:
   - added rows for `BEA Open Data API`, `BLS LPC`, `EIA STEO`, `Census BFS + BDS API`, and `BIS RPP + CPP + GLI`.

4. Expanded `12. Source notes`:
   - added source-family bullets for `BLS LPC`, `EIA STEO`, `Census BFS/BDS`, `BIS RPP/CPP/GLI`, and `BEA Open Data API`.

## Why this is useful

- Improves practical decision quality by linking ownership-entry signals to durability, valuation regime, and liquidity regime in one workflow.
- Tightens the bridge between question-bank IDs (`U31`-`U35`) and executable book guidance.
- Increases source-family coverage without adding speculative claims.
